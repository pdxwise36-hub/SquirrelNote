<?xml version="1.0" encoding="utf-8"?>
<!--
  Kept deliberately simple: RemoteViews (what widgets render with) only
  supports a small, fixed set of view types, no arbitrary web content,
  no live text input. The whole layout is one tap target that jumps
  straight into the real brain dump editor in the app; typing happens
  there, not in the widget itself.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/braindump_widget_bg"
    android:padding="14dp">

    <TextView
        android:id="@+id/widget_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="🧠 Brain dump"
        android:textColor="#F0E6D3"
        android:textSize="13sp"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/widget_preview"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:layout_marginTop="6dp"
        android:text="@string/braindump_widget_placeholder"
        android:textColor="#C9BCA0"
        android:textSize="12sp"
        android:maxLines="4"
        android:ellipsize="end" />

</LinearLayout>
